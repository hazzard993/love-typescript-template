{
  "name": "love-typescript-boilerplate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "lovec game",
    "build": "tstl -p tsconfig.json",
    "build-and-start": "npm run build && npm start",
    "watch": "tstl -w -p tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "love-typescript-definitions": "^0.9.9",
    "typescript-to-lua": "^0.19.0"
  },
  "dependencies": {}
}
