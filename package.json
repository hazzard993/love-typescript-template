{
  "name": "love-typescript-boilerplate",
  "scripts": {
    "start": "love game --console",
    "build": "tstl -p tsconfig.json",
    "build-and-start": "yarn build && yarn start",
    "watch": "tstl -w -p tsconfig.json"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "love-typescript-definitions": "^0.15.0",
    "lua-types": "^2.5.2",
    "typescript-to-lua": "^0.28.1"
  }
}
